cl_methods:
  ewc_lambda: 5000
  replay_buffer_size: 50
dataset:
  channels: 12
  classes: 2
  name: PTB-XL-Simulated
  num_patients: 5
  signal_length: 1000
  stream_length: 20
model:
  in_channels: 12
  name: ResNet1D
  num_classes: 5
project: ContinualTwin
training:
  batch_size: 32
  lr: 0.001
  population_epochs: 7
  stream_epochs_per_step: 3
